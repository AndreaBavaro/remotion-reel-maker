# Dependencies
node_modules/

# Build output
out/
dist/

# Raw video & audio files (too large for git — sync via AirDrop/Drive)
*.MOV
*.mov
*.mp4
*.MP4
*.wav
*.mp3
*.m4a

# Images (sync via AirDrop/Drive with the clips)
*.png
*.PNG
*.jpg
*.JPG
*.jpeg
*.JPEG
*.heic
*.HEIC

# Whisper (local tool, not part of the project)
whisper.cpp/

# Per-reel compositions are personal/local — each user generates their own
# via the `edit reelN` workflow. Only the shared infra is tracked.
src/reels/reel*/

# Generated docs (deliverables, not part of the repo)
*.pdf
*.docx

# One-off scripts
generate_marketing_pdf.py

# Generated build artifacts (Claude regenerates these)
public/reels/*/captions.json
public/reels/*/voiceover.wav
public/reels/*/build-log.md
public/reels/*/claude-output.txt
public/reels/*/spec.json
public/reels/*/transcriptions.json
public/reels/*/brief.txt

# Legacy reels/ directory (everything lives in public/reels/ now)
reels/

# One-off local files
tmp.json
transcribe-clips.ts
transcribe-reel1.command

# OS files
.DS_Store
Thumbs.db

# Environment
.env
.env.local
